<%= person.name %>,
    <%= person.age %> <%= person.id %> <%= person.createdAt %>
        <form action="/person/delete/<%= person.id %>" method="POST">
            <button type="submit" class="btn btn-Secondary">Delete</button>
        </form>